case ('ALL', 'all')
  if (.not.allocated(this%AREA_M2)) allocate(this%AREA_M2(nx,ny))
  if (.not.allocated(this%FRVEG)) allocate(this%FRVEG(nx,ny))
  if (.not.allocated(this%FRLAKE)) allocate(this%FRLAKE(nx,ny))
  if (.not.allocated(this%FRLAND)) allocate(this%FRLAND(nx,ny))
  if (.not.allocated(this%FRLANDIC)) allocate(this%FRLANDIC(nx,ny))
  if (.not.allocated(this%FROCEAN)) allocate(this%FROCEAN(nx,ny))
  if (.not.allocated(this%FRSEAICE)) allocate(this%FRSEAICE(nx,ny))
  if (.not.allocated(this%FRSNO)) allocate(this%FRSNO(nx,ny))
  if (.not.allocated(this%LAI)) allocate(this%LAI(nx,ny))
  if (.not.allocated(this%GVF)) allocate(this%GVF(nx,ny))
  if (.not.allocated(this%RDRAG)) allocate(this%RDRAG(nx,ny))
  if (.not.allocated(this%USTAR_THRESHOLD)) allocate(this%USTAR_THRESHOLD(nx,ny))
  if (.not.allocated(this%SSM)) allocate(this%SSM(nx,ny))
  if (.not.allocated(this%SEAICE00)) allocate(this%SEAICE00(nx,ny))
  if (.not.allocated(this%SEAICE10)) allocate(this%SEAICE10(nx,ny))
  if (.not.allocated(this%SEAICE20)) allocate(this%SEAICE20(nx,ny))
  if (.not.allocated(this%SEAICE30)) allocate(this%SEAICE30(nx,ny))
  if (.not.allocated(this%SEAICE40)) allocate(this%SEAICE40(nx,ny))
  if (.not.allocated(this%SEAICE50)) allocate(this%SEAICE50(nx,ny))
  if (.not.allocated(this%SEAICE60)) allocate(this%SEAICE60(nx,ny))
  if (.not.allocated(this%SEAICE70)) allocate(this%SEAICE70(nx,ny))
  if (.not.allocated(this%SEAICE80)) allocate(this%SEAICE80(nx,ny))
  if (.not.allocated(this%SEAICE90)) allocate(this%SEAICE90(nx,ny))
  if (.not.allocated(this%SNODP)) allocate(this%SNODP(nx,ny))
  if (.not.allocated(this%SNOMAS)) allocate(this%SNOMAS(nx,ny))
  if (.not.allocated(this%CLAYFRAC)) allocate(this%CLAYFRAC(nx,ny))
  if (.not.allocated(this%SANDFRAC)) allocate(this%SANDFRAC(nx,ny))
  if (.not.allocated(this%GWETTOP)) allocate(this%GWETTOP(nx,ny))
  if (.not.allocated(this%GWETROOT)) allocate(this%GWETROOT(nx,ny))
  if (.not.allocated(this%WILT)) allocate(this%WILT(nx,ny))
  if (.not.allocated(this%SOILM)) allocate(this%SOILM(nx,ny,nsoil))
  if (.not.allocated(this%FRLANDUSE)) allocate(this%FRLANDUSE(nx,ny,nSURFTYPE))
  if (.not.allocated(this%FRSOIL)) allocate(this%FRSOIL(nx,ny,nsoiltype))
  if (.not.allocated(this%FRLAI)) allocate(this%FRLAI(nx,ny,nSURFTYPE))
  if (.not.allocated(this%LAT)) allocate(this%LAT(nx,ny))
  if (.not.allocated(this%LON)) allocate(this%LON(nx,ny))
  if (.not.allocated(this%ALBD_VIS)) allocate(this%ALBD_VIS(nx,ny))
  if (.not.allocated(this%ALBD_NIR)) allocate(this%ALBD_NIR(nx,ny))
  if (.not.allocated(this%ALBD_UV)) allocate(this%ALBD_UV(nx,ny))
  if (.not.allocated(this%PARDR)) allocate(this%PARDR(nx,ny))
  if (.not.allocated(this%PARDF)) allocate(this%PARDF(nx,ny))
  if (.not.allocated(this%SUNCOS)) allocate(this%SUNCOS(nx,ny))
  if (.not.allocated(this%SUNCOSmid)) allocate(this%SUNCOSmid(nx,ny))
  if (.not.allocated(this%SUNCOSsum)) allocate(this%SUNCOSsum(nx,ny))
  if (.not.allocated(this%SZAFACT)) allocate(this%SZAFACT(nx,ny))
  if (.not.allocated(this%SWGDN)) allocate(this%SWGDN(nx,ny))
  if (.not.allocated(this%EFLUX)) allocate(this%EFLUX(nx,ny))
  if (.not.allocated(this%HFLUX)) allocate(this%HFLUX(nx,ny))
  if (.not.allocated(this%U10M)) allocate(this%U10M(nx,ny))
  if (.not.allocated(this%USTAR)) allocate(this%USTAR(nx,ny))
  if (.not.allocated(this%V10M)) allocate(this%V10M(nx,ny))
  if (.not.allocated(this%Z0)) allocate(this%Z0(nx,ny))
  if (.not.allocated(this%Z0H)) allocate(this%Z0H(nx,ny))
  if (.not.allocated(this%FRZ0)) allocate(this%FRZ0(nx,ny,nSURFTYPE))
  if (.not.allocated(this%PBLH)) allocate(this%PBLH(nx,ny))
  if (.not.allocated(this%F_OF_PBL)) allocate(this%F_OF_PBL(nx,ny,nz))
  if (.not.allocated(this%F_UNDER_PBLTOP)) allocate(this%F_UNDER_PBLTOP(nx,ny,nz))
  if (.not.allocated(this%OBK)) allocate(this%OBK(nx,ny))
  if (.not.allocated(this%CLDFRC)) allocate(this%CLDFRC(nx,ny))
  if (.not.allocated(this%CONV_DEPTH)) allocate(this%CONV_DEPTH(nx,ny))
  if (.not.allocated(this%FLASH_DENS)) allocate(this%FLASH_DENS(nx,ny))
  if (.not.allocated(this%CNV_FRC)) allocate(this%CNV_FRC(nx,ny))
  if (.not.allocated(this%CLDF)) allocate(this%CLDF(nx,ny,nz))
  if (.not.allocated(this%CMFMC)) allocate(this%CMFMC(nx,ny,nz))
  if (.not.allocated(this%DQRCU)) allocate(this%DQRCU(nx,ny,nz))
  if (.not.allocated(this%DQRLSAN)) allocate(this%DQRLSAN(nx,ny,nz))
  if (.not.allocated(this%DTRAIN)) allocate(this%DTRAIN(nx,ny,nz))
  if (.not.allocated(this%PRECANV)) allocate(this%PRECANV(nx,ny))
  if (.not.allocated(this%PRECCON)) allocate(this%PRECCON(nx,ny))
  if (.not.allocated(this%PRECLSC)) allocate(this%PRECLSC(nx,ny))
  if (.not.allocated(this%QI)) allocate(this%QI(nx,ny,nz))
  if (.not.allocated(this%QL)) allocate(this%QL(nx,ny,nz))
  if (.not.allocated(this%PFICU)) allocate(this%PFICU(nx,ny,nz))
  if (.not.allocated(this%PFILSAN)) allocate(this%PFILSAN(nx,ny,nz))
  if (.not.allocated(this%PFLCU)) allocate(this%PFLCU(nx,ny,nz))
  if (.not.allocated(this%PFLLSAN)) allocate(this%PFLLSAN(nx,ny,nz))
  if (.not.allocated(this%TAUCLI)) allocate(this%TAUCLI(nx,ny,nz))
  if (.not.allocated(this%TAUCLW)) allocate(this%TAUCLW(nx,ny,nz))
  if (.not.allocated(this%PHIS)) allocate(this%PHIS(nx,ny))
  if (.not.allocated(this%PS_WET)) allocate(this%PS_WET(nx,ny))
  if (.not.allocated(this%PS_DRY)) allocate(this%PS_DRY(nx,ny))
  if (.not.allocated(this%QV2M)) allocate(this%QV2M(nx,ny))
  if (.not.allocated(this%T2M)) allocate(this%T2M(nx,ny))
  if (.not.allocated(this%TS)) allocate(this%TS(nx,ny))
  if (.not.allocated(this%TSKIN)) allocate(this%TSKIN(nx,ny))
  if (.not.allocated(this%SST)) allocate(this%SST(nx,ny))
  if (.not.allocated(this%SLP)) allocate(this%SLP(nx,ny))
  if (.not.allocated(this%PS)) allocate(this%PS(nx,ny))
  if (.not.allocated(this%TO3)) allocate(this%TO3(nx,ny))
  if (.not.allocated(this%TROPP)) allocate(this%TROPP(nx,ny))
  if (.not.allocated(this%TropHt)) allocate(this%TropHt(nx,ny))
  if (.not.allocated(this%Z)) allocate(this%Z(nx,ny,nz))
  if (.not.allocated(this%ZMID)) allocate(this%ZMID(nx,ny,nz))
  if (.not.allocated(this%BXHEIGHT)) allocate(this%BXHEIGHT(nx,ny,nz))
  if (.not.allocated(this%QV)) allocate(this%QV(nx,ny,nz))
  if (.not.allocated(this%T)) allocate(this%T(nx,ny,nz))
  if (.not.allocated(this%THETA)) allocate(this%THETA(nx,ny,nz))
  if (.not.allocated(this%TV)) allocate(this%TV(nx,ny,nz))
  if (.not.allocated(this%V)) allocate(this%V(nx,ny,nz))
  if (.not.allocated(this%U)) allocate(this%U(nx,ny,nz))
  if (.not.allocated(this%OMEGA)) allocate(this%OMEGA(nx,ny,nz))
  if (.not.allocated(this%RH)) allocate(this%RH(nx,ny,nz))
  if (.not.allocated(this%SPHU)) allocate(this%SPHU(nx,ny,nz))
  if (.not.allocated(this%AIRDEN)) allocate(this%AIRDEN(nx,ny,nz))
  if (.not.allocated(this%AIRNUMDEN)) allocate(this%AIRNUMDEN(nx,ny,nz))
  if (.not.allocated(this%MAIRDEN)) allocate(this%MAIRDEN(nx,ny,nz))
  if (.not.allocated(this%AVGW)) allocate(this%AVGW(nx,ny,nz))
  if (.not.allocated(this%DELP)) allocate(this%DELP(nx,ny,nz))
  if (.not.allocated(this%DELP_DRY)) allocate(this%DELP_DRY(nx,ny,nz))
  if (.not.allocated(this%DAIRMASS)) allocate(this%DAIRMASS(nx,ny,nz))
  if (.not.allocated(this%AIRVOL)) allocate(this%AIRVOL(nx,ny,nz))
  if (.not.allocated(this%PEDGE_DRY)) allocate(this%PEDGE_DRY(nx,ny,nz+1))  ! Edge field
  if (.not.allocated(this%PEDGE)) allocate(this%PEDGE(nx,ny,nz+1))  ! Edge field
  if (.not.allocated(this%PMID)) allocate(this%PMID(nx,ny,nz))
  if (.not.allocated(this%PMID_DRY)) allocate(this%PMID_DRY(nx,ny,nz))
case ('AREA_M2', 'area_m2')
  if (.not.allocated(this%AREA_M2)) allocate(this%AREA_M2(nx,ny))
case ('FRVEG', 'frveg')
  if (.not.allocated(this%FRVEG)) allocate(this%FRVEG(nx,ny))
case ('FRLAKE', 'frlake')
  if (.not.allocated(this%FRLAKE)) allocate(this%FRLAKE(nx,ny))
case ('FRLAND', 'frland')
  if (.not.allocated(this%FRLAND)) allocate(this%FRLAND(nx,ny))
case ('FRLANDIC', 'frlandic')
  if (.not.allocated(this%FRLANDIC)) allocate(this%FRLANDIC(nx,ny))
case ('FROCEAN', 'frocean')
  if (.not.allocated(this%FROCEAN)) allocate(this%FROCEAN(nx,ny))
case ('FRSEAICE', 'frseaice')
  if (.not.allocated(this%FRSEAICE)) allocate(this%FRSEAICE(nx,ny))
case ('FRSNO', 'frsno')
  if (.not.allocated(this%FRSNO)) allocate(this%FRSNO(nx,ny))
case ('LAI', 'lai')
  if (.not.allocated(this%LAI)) allocate(this%LAI(nx,ny))
case ('GVF', 'gvf')
  if (.not.allocated(this%GVF)) allocate(this%GVF(nx,ny))
case ('RDRAG', 'rdrag')
  if (.not.allocated(this%RDRAG)) allocate(this%RDRAG(nx,ny))
case ('USTAR_THRESHOLD', 'ustar_threshold')
  if (.not.allocated(this%USTAR_THRESHOLD)) allocate(this%USTAR_THRESHOLD(nx,ny))
case ('SSM', 'ssm')
  if (.not.allocated(this%SSM)) allocate(this%SSM(nx,ny))
case ('SEAICE00', 'seaice00')
  if (.not.allocated(this%SEAICE00)) allocate(this%SEAICE00(nx,ny))
case ('SEAICE10', 'seaice10')
  if (.not.allocated(this%SEAICE10)) allocate(this%SEAICE10(nx,ny))
case ('SEAICE20', 'seaice20')
  if (.not.allocated(this%SEAICE20)) allocate(this%SEAICE20(nx,ny))
case ('SEAICE30', 'seaice30')
  if (.not.allocated(this%SEAICE30)) allocate(this%SEAICE30(nx,ny))
case ('SEAICE40', 'seaice40')
  if (.not.allocated(this%SEAICE40)) allocate(this%SEAICE40(nx,ny))
case ('SEAICE50', 'seaice50')
  if (.not.allocated(this%SEAICE50)) allocate(this%SEAICE50(nx,ny))
case ('SEAICE60', 'seaice60')
  if (.not.allocated(this%SEAICE60)) allocate(this%SEAICE60(nx,ny))
case ('SEAICE70', 'seaice70')
  if (.not.allocated(this%SEAICE70)) allocate(this%SEAICE70(nx,ny))
case ('SEAICE80', 'seaice80')
  if (.not.allocated(this%SEAICE80)) allocate(this%SEAICE80(nx,ny))
case ('SEAICE90', 'seaice90')
  if (.not.allocated(this%SEAICE90)) allocate(this%SEAICE90(nx,ny))
case ('SNODP', 'snodp')
  if (.not.allocated(this%SNODP)) allocate(this%SNODP(nx,ny))
case ('SNOMAS', 'snomas')
  if (.not.allocated(this%SNOMAS)) allocate(this%SNOMAS(nx,ny))
case ('CLAYFRAC', 'clayfrac')
  if (.not.allocated(this%CLAYFRAC)) allocate(this%CLAYFRAC(nx,ny))
case ('SANDFRAC', 'sandfrac')
  if (.not.allocated(this%SANDFRAC)) allocate(this%SANDFRAC(nx,ny))
case ('GWETTOP', 'gwettop')
  if (.not.allocated(this%GWETTOP)) allocate(this%GWETTOP(nx,ny))
case ('GWETROOT', 'gwetroot')
  if (.not.allocated(this%GWETROOT)) allocate(this%GWETROOT(nx,ny))
case ('WILT', 'wilt')
  if (.not.allocated(this%WILT)) allocate(this%WILT(nx,ny))
case ('SOILM', 'soilm')
  if (.not.allocated(this%SOILM)) allocate(this%SOILM(nx,ny,nsoil))
case ('FRLANDUSE', 'frlanduse')
  if (.not.allocated(this%FRLANDUSE)) allocate(this%FRLANDUSE(nx,ny,nSURFTYPE))
case ('FRSOIL', 'frsoil')
  if (.not.allocated(this%FRSOIL)) allocate(this%FRSOIL(nx,ny,nsoiltype))
case ('FRLAI', 'frlai')
  if (.not.allocated(this%FRLAI)) allocate(this%FRLAI(nx,ny,nSURFTYPE))
case ('LAT', 'lat')
  if (.not.allocated(this%LAT)) allocate(this%LAT(nx,ny))
case ('LON', 'lon')
  if (.not.allocated(this%LON)) allocate(this%LON(nx,ny))
case ('ALBD_VIS', 'albd_vis')
  if (.not.allocated(this%ALBD_VIS)) allocate(this%ALBD_VIS(nx,ny))
case ('ALBD_NIR', 'albd_nir')
  if (.not.allocated(this%ALBD_NIR)) allocate(this%ALBD_NIR(nx,ny))
case ('ALBD_UV', 'albd_uv')
  if (.not.allocated(this%ALBD_UV)) allocate(this%ALBD_UV(nx,ny))
case ('PARDR', 'pardr')
  if (.not.allocated(this%PARDR)) allocate(this%PARDR(nx,ny))
case ('PARDF', 'pardf')
  if (.not.allocated(this%PARDF)) allocate(this%PARDF(nx,ny))
case ('SUNCOS', 'suncos')
  if (.not.allocated(this%SUNCOS)) allocate(this%SUNCOS(nx,ny))
case ('SUNCOSmid', 'suncosmid')
  if (.not.allocated(this%SUNCOSmid)) allocate(this%SUNCOSmid(nx,ny))
case ('SUNCOSsum', 'suncossum')
  if (.not.allocated(this%SUNCOSsum)) allocate(this%SUNCOSsum(nx,ny))
case ('SZAFACT', 'szafact')
  if (.not.allocated(this%SZAFACT)) allocate(this%SZAFACT(nx,ny))
case ('SWGDN', 'swgdn')
  if (.not.allocated(this%SWGDN)) allocate(this%SWGDN(nx,ny))
case ('EFLUX', 'eflux')
  if (.not.allocated(this%EFLUX)) allocate(this%EFLUX(nx,ny))
case ('HFLUX', 'hflux')
  if (.not.allocated(this%HFLUX)) allocate(this%HFLUX(nx,ny))
case ('U10M', 'u10m')
  if (.not.allocated(this%U10M)) allocate(this%U10M(nx,ny))
case ('USTAR', 'ustar')
  if (.not.allocated(this%USTAR)) allocate(this%USTAR(nx,ny))
case ('V10M', 'v10m')
  if (.not.allocated(this%V10M)) allocate(this%V10M(nx,ny))
case ('Z0', 'z0')
  if (.not.allocated(this%Z0)) allocate(this%Z0(nx,ny))
case ('Z0H', 'z0h')
  if (.not.allocated(this%Z0H)) allocate(this%Z0H(nx,ny))
case ('FRZ0', 'frz0')
  if (.not.allocated(this%FRZ0)) allocate(this%FRZ0(nx,ny,nSURFTYPE))
case ('PBLH', 'pblh')
  if (.not.allocated(this%PBLH)) allocate(this%PBLH(nx,ny))
case ('F_OF_PBL', 'f_of_pbl')
  if (.not.allocated(this%F_OF_PBL)) allocate(this%F_OF_PBL(nx,ny,nz))
case ('F_UNDER_PBLTOP', 'f_under_pbltop')
  if (.not.allocated(this%F_UNDER_PBLTOP)) allocate(this%F_UNDER_PBLTOP(nx,ny,nz))
case ('OBK', 'obk')
  if (.not.allocated(this%OBK)) allocate(this%OBK(nx,ny))
case ('CLDFRC', 'cldfrc')
  if (.not.allocated(this%CLDFRC)) allocate(this%CLDFRC(nx,ny))
case ('CONV_DEPTH', 'conv_depth')
  if (.not.allocated(this%CONV_DEPTH)) allocate(this%CONV_DEPTH(nx,ny))
case ('FLASH_DENS', 'flash_dens')
  if (.not.allocated(this%FLASH_DENS)) allocate(this%FLASH_DENS(nx,ny))
case ('CNV_FRC', 'cnv_frc')
  if (.not.allocated(this%CNV_FRC)) allocate(this%CNV_FRC(nx,ny))
case ('CLDF', 'cldf')
  if (.not.allocated(this%CLDF)) allocate(this%CLDF(nx,ny,nz))
case ('CMFMC', 'cmfmc')
  if (.not.allocated(this%CMFMC)) allocate(this%CMFMC(nx,ny,nz))
case ('DQRCU', 'dqrcu')
  if (.not.allocated(this%DQRCU)) allocate(this%DQRCU(nx,ny,nz))
case ('DQRLSAN', 'dqrlsan')
  if (.not.allocated(this%DQRLSAN)) allocate(this%DQRLSAN(nx,ny,nz))
case ('DTRAIN', 'dtrain')
  if (.not.allocated(this%DTRAIN)) allocate(this%DTRAIN(nx,ny,nz))
case ('PRECANV', 'precanv')
  if (.not.allocated(this%PRECANV)) allocate(this%PRECANV(nx,ny))
case ('PRECCON', 'preccon')
  if (.not.allocated(this%PRECCON)) allocate(this%PRECCON(nx,ny))
case ('PRECLSC', 'preclsc')
  if (.not.allocated(this%PRECLSC)) allocate(this%PRECLSC(nx,ny))
case ('QI', 'qi')
  if (.not.allocated(this%QI)) allocate(this%QI(nx,ny,nz))
case ('QL', 'ql')
  if (.not.allocated(this%QL)) allocate(this%QL(nx,ny,nz))
case ('PFICU', 'pficu')
  if (.not.allocated(this%PFICU)) allocate(this%PFICU(nx,ny,nz))
case ('PFILSAN', 'pfilsan')
  if (.not.allocated(this%PFILSAN)) allocate(this%PFILSAN(nx,ny,nz))
case ('PFLCU', 'pflcu')
  if (.not.allocated(this%PFLCU)) allocate(this%PFLCU(nx,ny,nz))
case ('PFLLSAN', 'pfllsan')
  if (.not.allocated(this%PFLLSAN)) allocate(this%PFLLSAN(nx,ny,nz))
case ('TAUCLI', 'taucli')
  if (.not.allocated(this%TAUCLI)) allocate(this%TAUCLI(nx,ny,nz))
case ('TAUCLW', 'tauclw')
  if (.not.allocated(this%TAUCLW)) allocate(this%TAUCLW(nx,ny,nz))
case ('PHIS', 'phis')
  if (.not.allocated(this%PHIS)) allocate(this%PHIS(nx,ny))
case ('PS_WET', 'ps_wet')
  if (.not.allocated(this%PS_WET)) allocate(this%PS_WET(nx,ny))
case ('PS_DRY', 'ps_dry')
  if (.not.allocated(this%PS_DRY)) allocate(this%PS_DRY(nx,ny))
case ('QV2M', 'qv2m')
  if (.not.allocated(this%QV2M)) allocate(this%QV2M(nx,ny))
case ('T2M', 't2m')
  if (.not.allocated(this%T2M)) allocate(this%T2M(nx,ny))
case ('TS', 'ts')
  if (.not.allocated(this%TS)) allocate(this%TS(nx,ny))
case ('TSKIN', 'tskin')
  if (.not.allocated(this%TSKIN)) allocate(this%TSKIN(nx,ny))
case ('SST', 'sst')
  if (.not.allocated(this%SST)) allocate(this%SST(nx,ny))
case ('SLP', 'slp')
  if (.not.allocated(this%SLP)) allocate(this%SLP(nx,ny))
case ('PS', 'ps')
  if (.not.allocated(this%PS)) allocate(this%PS(nx,ny))
case ('TO3', 'to3')
  if (.not.allocated(this%TO3)) allocate(this%TO3(nx,ny))
case ('TROPP', 'tropp')
  if (.not.allocated(this%TROPP)) allocate(this%TROPP(nx,ny))
case ('TropHt', 'tropht')
  if (.not.allocated(this%TropHt)) allocate(this%TropHt(nx,ny))
case ('Z', 'z')
  if (.not.allocated(this%Z)) allocate(this%Z(nx,ny,nz))
case ('ZMID', 'zmid')
  if (.not.allocated(this%ZMID)) allocate(this%ZMID(nx,ny,nz))
case ('BXHEIGHT', 'bxheight')
  if (.not.allocated(this%BXHEIGHT)) allocate(this%BXHEIGHT(nx,ny,nz))
case ('QV', 'qv')
  if (.not.allocated(this%QV)) allocate(this%QV(nx,ny,nz))
case ('T', 't')
  if (.not.allocated(this%T)) allocate(this%T(nx,ny,nz))
case ('THETA', 'theta')
  if (.not.allocated(this%THETA)) allocate(this%THETA(nx,ny,nz))
case ('TV', 'tv')
  if (.not.allocated(this%TV)) allocate(this%TV(nx,ny,nz))
case ('V', 'v')
  if (.not.allocated(this%V)) allocate(this%V(nx,ny,nz))
case ('U', 'u')
  if (.not.allocated(this%U)) allocate(this%U(nx,ny,nz))
case ('OMEGA', 'omega')
  if (.not.allocated(this%OMEGA)) allocate(this%OMEGA(nx,ny,nz))
case ('RH', 'rh')
  if (.not.allocated(this%RH)) allocate(this%RH(nx,ny,nz))
case ('SPHU', 'sphu')
  if (.not.allocated(this%SPHU)) allocate(this%SPHU(nx,ny,nz))
case ('AIRDEN', 'airden')
  if (.not.allocated(this%AIRDEN)) allocate(this%AIRDEN(nx,ny,nz))
case ('AIRNUMDEN', 'airnumden')
  if (.not.allocated(this%AIRNUMDEN)) allocate(this%AIRNUMDEN(nx,ny,nz))
case ('MAIRDEN', 'mairden')
  if (.not.allocated(this%MAIRDEN)) allocate(this%MAIRDEN(nx,ny,nz))
case ('AVGW', 'avgw')
  if (.not.allocated(this%AVGW)) allocate(this%AVGW(nx,ny,nz))
case ('DELP', 'delp')
  if (.not.allocated(this%DELP)) allocate(this%DELP(nx,ny,nz))
case ('DELP_DRY', 'delp_dry')
  if (.not.allocated(this%DELP_DRY)) allocate(this%DELP_DRY(nx,ny,nz))
case ('DAIRMASS', 'dairmass')
  if (.not.allocated(this%DAIRMASS)) allocate(this%DAIRMASS(nx,ny,nz))
case ('AIRVOL', 'airvol')
  if (.not.allocated(this%AIRVOL)) allocate(this%AIRVOL(nx,ny,nz))
case ('PEDGE_DRY', 'pedge_dry')
  if (.not.allocated(this%PEDGE_DRY)) allocate(this%PEDGE_DRY(nx,ny,nz+1))  ! Edge field
case ('PEDGE', 'pedge')
  if (.not.allocated(this%PEDGE)) allocate(this%PEDGE(nx,ny,nz+1))  ! Edge field
case ('PMID', 'pmid')
  if (.not.allocated(this%PMID)) allocate(this%PMID(nx,ny,nz))
case ('PMID_DRY', 'pmid_dry')
  if (.not.allocated(this%PMID_DRY)) allocate(this%PMID_DRY(nx,ny,nz))
case default
  ! No allocation for unknown field
