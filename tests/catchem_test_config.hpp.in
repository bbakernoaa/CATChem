#pragma once
#include <string_view>

namespace catchem {
    namespace test {
        constexpr std::string_view SOURCE_DIR = "@PROJECT_SOURCE_DIR@";
        constexpr std::string_view TEST_DIR = "@CMAKE_CURRENT_SOURCE_DIR@";
        // Thread count the Kokkos-capable tests are pinned to via CTest's
        // OMP_NUM_THREADS environment (single knob: CATCHEM_TEST_OMP_THREADS).
        constexpr int EXPECTED_OMP_THREADS = @CATCHEM_TEST_OMP_THREADS@;
    }
}
